import { PetCard } from "../../components/Pet/PetCard";

export default function HelpAnimalsPage() {
  return (
    <>
      <h1>Help animals!</h1>
      <div>
        <ul>
          {/* {pets?.map((pet) => (
            <li key={pet.id}>
              <PetCard pet={pet} />
            </li>
          ))} */}
        </ul>
      </div>
    </>
  );
}
